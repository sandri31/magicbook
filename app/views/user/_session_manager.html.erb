<% if current_user %>
  <ul class="navbar-nav">
    <li class="nav-item dropdown">
      <a class="btn btn-info dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= current_user.email %>
      </a>
      <ul class="dropdown-menu">
        <li><a class="dropdown-item" href="<%= edit_user_registration_path %>">Modifier mon profil</a></li>
        <li><a> <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %></a></li>
      </ul>
    </li>
  </ul>
<% else %>
  <%= link_to "Connexion", new_user_session_path, class: "btn btn-primary me-2" %>
  <%= link_to "Inscription", new_user_registration_path, class: "btn btn-success me-2" %>
<% end %>

<% if current_user %>
  <ul class="navbar-nav">
    <li class="nav-item dropdown">
      <a class="getstarted scrollto dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= current_user.username.capitalize %>
      </a>
      <ul class="dropdown-menu">
        <li><%= button_to "Mon profil", edit_user_registration_path, class: "dropdown-item" %></li>
        <li><%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
      </ul>
    </li>
  </ul>
<% else %>
  <%= link_to "Connexion", new_user_session_path, class: "getstarted scrollto" %>
  &nbsp;
  <%= link_to "Inscription", new_user_registration_path, class: "getstarted scrollto" %>
<% end %>
